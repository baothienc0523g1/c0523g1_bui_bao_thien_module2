package z_bai_tap;

public class ZzzzTest {
//    private List<Villa> getVillaFromCSV() {
//        List<Villa> villas = new ArrayList<>();
//        List<String> strings = myReadFromCSV(VILLA_PATH);
//        String[] pointer = null;
//
//        for (String s : strings) {
//            Villa villa = new Villa(pointer[0], pointer[1], Double.parseDouble(pointer[2]),
//                    Double.parseDouble(pointer[3]), Integer.parseInt(pointer[4]),
//                    pointer[5], pointer[6], Double.parseDouble(pointer[7]),
//                    Integer.parseInt(pointer[8]));
//            villas.add(villa);
//        }
//        return villas;
//    }
//
//    private List<House> getHouseFromCSV() {
//        List<House> houses = new ArrayList<>();
//        List<String> strings = myReadFromCSV(HOUSE_PATH);
//        String[] pointer = null;
//
//        for (String s : strings) {
//            House house = new House(pointer[0], pointer[1], Double.parseDouble(pointer[2]),
//                    Double.parseDouble(pointer[3]), Integer.parseInt(pointer[4]),
//                    pointer[5], pointer[6], Integer.parseInt(pointer[7]));
//            houses.add(house);
//        }
//        return houses;
//    }
//    private List<Room> getRoomFromCSV() {
//        List<Room> rooms = new ArrayList<>();
//        List<String> strings = myReadFromCSV(ROOM_PATH);
//        String[] pointer = null;
//
//        for (String s : strings) {
//            Room room = new Room(pointer[0], pointer[1], Double.parseDouble(pointer[2]),
//                    Double.parseDouble(pointer[3]), Integer.parseInt(pointer[4]),
//                    pointer[5], pointer[6]);
//            rooms.add(room);
//        }
//        return rooms;
//    }
    /////////////////////
    //    @Override
//    public Map<Facility, Integer> getList() {
//        List<String> stringList = myReadFromCSV(FACILITY_PATH);
//        Map<Facility, Integer> facilities = new LinkedHashMap<>();
//        String[] pointer;
//        for (String str : stringList) {
//            pointer = str.split(",");
//            if (pointer.length > 9) {
//                facilities.put(new Villa(pointer[0], (pointer[1]), Double.parseDouble(pointer[2]),
//                        Double.parseDouble(pointer[3]), Integer.parseInt(pointer[4]),
//                        pointer[5], pointer[6], Double.parseDouble(pointer[7]),
//                        Integer.parseInt(pointer[8])), Integer.parseInt(pointer[9]));
//            } else if (pointer.length == 9) {
//                facilities.put(new House(pointer[0], (pointer[1]), Double.parseDouble(pointer[2]),
//                        Double.parseDouble(pointer[3]), Integer.parseInt(pointer[4]),
//                        pointer[5], pointer[6], Integer.parseInt(pointer[7])), Integer.parseInt(pointer[8]));
//            } else {
//                facilities.put(new Room(pointer[0], (pointer[1]), Double.parseDouble(pointer[2]),
//                        Double.parseDouble(pointer[3]), Integer.parseInt(pointer[4]),
//                        pointer[5], pointer[6]), Integer.parseInt(pointer[7]));
//            }
//        }
//        return facilities;
//    }
}
